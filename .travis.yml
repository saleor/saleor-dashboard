dist: xenial
language: node_js
sudo: false

node_js:
  - "12.12"

branches:
  only:
    - master
    - /next\/.*/

jobs:
  include:
    # - name: Build
    #   script: npm run build
    - name: Test
      script:
        - sh .travis/check-strict-null-errors.sh
        # - npm test -- --ci --coverage

env:
  global:
    - APP_MOUNT_URI="/"
    - API_URI="/graphql/"

after_success:
  - codecov
