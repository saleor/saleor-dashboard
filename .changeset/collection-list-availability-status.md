---
"saleor-dashboard": patch
---

On the collections list, availability uses a quieter status indicator (like products) instead of a red “No channels” label. Collections that are unpublished or have no channels show a warning style rather than an error style.
