---
"saleor-dashboard": patch
---

Collection list availability uses the quieter status-dot treatment (like products) instead of a red “No channels” pill; unpublished / empty states use warning rather than critical.
