---
"saleor-dashboard": patch
---

Your models list is now grouped by type, with tabs across the top so you can focus on one kind at a time (e.g. brands, refund reasons) instead of one long mixed list. You can pin types you use often so they stay easy to reach, and Create model suggests the type that matches the tab you’re on.