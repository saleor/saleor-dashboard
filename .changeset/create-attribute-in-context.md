---
"saleor-dashboard": patch
---

Add in-context attribute creation on model type detail pages. Instead of navigating to the Model Attributes section, merchants can create and assign an attribute from a two-step modal wizard directly on the model type page.
