---
"saleor-dashboard": patch
---

Order “add product” dialog paginates variants per product (50 at a time) with Load more, clearer priced-vs-loaded progress, and a proper loading state—instead of truncating large catalogs with no way to reach the rest.
