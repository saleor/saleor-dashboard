---
"saleor-dashboard": patch
---

The Product Rating field on the product details page is now marked as deprecated. Product rating is being removed from the API — create a numeric "rating" attribute instead. The field still works for now.
