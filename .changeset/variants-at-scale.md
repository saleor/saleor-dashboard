---
"saleor-dashboard": patch
---

Dashboard stays usable with large variant catalogs: product variants load in pages with search, sibling navigation and the translations context switcher paginate instead of fetching every variant, order “add product” and Assign Variant dialogs offer Load more with clear progress, and Product Doctor walks a slim variant catalog for availability checks.