---
"saleor-dashboard": patch
---

Fix how order value shipping handles long carrier method names. Now the name is truncated (full text on hover), and **Change** sits next to Shipping instead of linking the raw method string.
