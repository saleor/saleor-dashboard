---
"saleor-dashboard": patch
---

Delete product now lives in the page menu (gear), with GraphiQL just above it — same pattern as collections and channels. The save bar no longer has a delete button.
