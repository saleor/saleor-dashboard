import { defineMessages } from "react-intl";

export const messages = defineMessages({
  editAttributeMetadata: {
    id: "nKZUy3",
    defaultMessage: "Edit attribute metadata",
    description: "attribute detail page, top-bar metadata button tooltip",
  },
});
