import { defineMessages } from "react-intl";

export const channelStatusLabelMessages = defineMessages({
  globalTitle: {
    defaultMessage: "Global plugin",
    description: "channel status label global title"
  },
  channelTitle: {
    defaultMessage: "Assigned to {activeChannelsCount}",
    description: "channel status label channel title"
  }
});
