import { defineMessages } from "react-intl";

export const translationsButtonMessages = defineMessages({
  openTranslations: {
    id: "mykruj",
    defaultMessage: "Open translations",
    description: "detail page top bar, translations button tooltip",
  },
});
