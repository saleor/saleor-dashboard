export const PLACEHOLDER = "-";
export const ROW_ACTION_BAR_WIDTH = 80;

/** Default Glide row-marker width; checkbox (~18px) is centered, so its left edge sits at ~15px. */
export const DEFAULT_ROW_MARKER_WIDTH = 48;

/**
 * Row-marker width that aligns the checkbox left edge with `paddingX={6}` (24px)
 * list chrome (search, pagination, tabs).
 */
export const LIST_INSET_ROW_MARKER_WIDTH = 66;
