import { defineMessages } from "react-intl";

export const constraintReasonMessages = defineMessages({
  neededFor: {
    id: "MKCw7u",
    defaultMessage: "Needed for filtering on {fields}",
    description:
      "Tooltip on the info icon before a locked Channel or Currency filter listing the filters that require it",
  },
});
