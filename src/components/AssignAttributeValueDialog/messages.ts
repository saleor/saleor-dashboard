import { defineMessages } from "react-intl";

export const messages = defineMessages({
  assignReferencesButton: {
    defaultMessage: "Assign",
    id: "ybWt/r",
    description: "assign reference attribute values to the form without saving the entity",
  },
});
