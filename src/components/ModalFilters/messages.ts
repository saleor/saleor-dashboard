import { defineMessages } from "react-intl";

export const messages = defineMessages({
  restrictedTo: {
    defaultMessage: "Showing only {label}:",
    id: "i1yrI2",
    description: "hint in picker modal when the list is restricted and no filters are available",
  },
});
