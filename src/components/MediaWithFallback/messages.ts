import { defineMessages } from "react-intl";

export const mediaFallbackMessages = defineMessages({
  imageLoadFailed: {
    id: "/UP01b",
    defaultMessage: "Image could not be loaded",
    description: "Fallback text shown when a product media image fails to load",
  },
});
