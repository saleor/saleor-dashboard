/**
 * PaymentMethodDetails component is no longer used since paymentMethodDetails
 * was removed from the TransactionItem GraphQL fragment.
 * Keeping as a no-op stub in case it's imported elsewhere.
 */
export const PaymentMethodDetails = (_props: { paymentMethodDetails?: unknown }) => {
  return null;
};
