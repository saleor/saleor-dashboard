/** Number of columns in the refunds GridTable. Kept in one place so the
 * colgroup, group spacer, "show all lines" bar and the timestamp header row
 * all span the table consistently. */
export const REFUND_TABLE_COLUMN_COUNT = 5;
