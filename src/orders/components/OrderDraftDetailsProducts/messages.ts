import { defineMessages } from "react-intl";

export const messages = defineMessages({
  productDeleted: {
    defaultMessage: "This product variant no longer exists",
    description: "deleted product info in an unconfirmed order"
  }
});
