import { defineMessages } from "react-intl";

export const messages = defineMessages({
  giftCardReturnOrReplaceNotAllowed: {
    defaultMessage:
      "You cannot return or replace this product because it is a gift card",
    description: "alert"
  }
});
