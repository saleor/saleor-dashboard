import { defineMessages } from "react-intl";

export const messages = defineMessages({
  giftCardRefundNotAllowed: {
    defaultMessage: "You cannot refund this product because it is a gift card",
    description: "alert"
  }
});
