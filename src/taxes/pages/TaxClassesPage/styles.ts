import { makeStyles } from "@saleor/macaw-ui";

export const useStyles = makeStyles(
  () => ({
    searchPadding: {
      padding: "16px 0 16px 0",
    },
    namePadding: {
      padding: "16px",
    },
  }),
  { name: "TaxClassesPage" },
);
