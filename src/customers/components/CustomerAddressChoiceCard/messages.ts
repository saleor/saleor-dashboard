import { defineMessages } from "react-intl";

export const addressChoiceCardMessages = defineMessages({
  editLabel: {
    defaultMessage: "Edit",
    description: "Edit label"
  },
  selectedLabel: {
    defaultMessage: "Selected",
    description: "Selected label"
  }
});
